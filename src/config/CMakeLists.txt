set(CONFIG_LIBS "" PARENT_SCOPE)

#For now it is header only
#file(GLOB config_SRC "*.cpp")
#add_library(config STATIC ${config_SRC})
#target_link_libraries(config ${Boost_LIBRARIES})
#target_include_directories(config PUBLIC ${Boost_INCLUDE_DIRS})
